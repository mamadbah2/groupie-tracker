{{ template "base" .}}

{{ define "content" }}

<div class="container">
    {{ range .AllArtist }}
    <a href="detail?Id={{ .Id }}">
        <div class="box">
            <div class="image">
                <img src="{{ .Image }}" alt="#">
            </div>
            <h3>
                {{ .Name }}
            </h3>
        </div>
    </a>
    {{ end }}
</div>

{{ end }}